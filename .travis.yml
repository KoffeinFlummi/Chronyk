language: python
python:
  - "2.7"
  - "3.2"
  - "3.3"
  - "3.4"

before_script:
  - pip install pytest
  - pip install coveralls
install:
  - python setup.py install
script:
  - coverage run --source=chronyk py.test
after_success:
  - coveralls
